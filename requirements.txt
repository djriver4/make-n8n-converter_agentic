# Python 3.10+ is required due to CrewAI's dependency on the pipe operator (|) for union types
# If you're using Python 3.9 or below, use conda to create an environment with Python 3.10+
# See environment.yml for a conda-compatible setup

crewai>=0.5.0
perplexityai>=1.0.5
requests>=2.31.0
pydantic>=2.4.2
tenacity>=8.1.0
langchain>=0.1.0
langchain-openai>=0.0.2
openai>=1.7.1
tiktoken>=0.5.2